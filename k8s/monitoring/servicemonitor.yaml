apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: taskgrid-api
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: taskgrid-api
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
      scrapeTimeout: 5s
